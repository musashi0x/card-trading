{
  "name": "@cardmkt/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "stellar contract build",
    "test": "cargo test",
    "deploy": "tsx scripts/deploy.ts",
    "upgrade": "tsx scripts/upgrade.ts",
    "lint": "cargo fmt --check || true",
    "typecheck": "echo \"(typecheck) rust crate — see cargo\""
  },
  "dependencies": {
    "@cardmkt/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
